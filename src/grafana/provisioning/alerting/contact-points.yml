# Contact points for alert notifications.
# Configure notification channels via the Grafana UI (Alerting → Contact points).
# To provision contact points, add them here and set corresponding environment
# variables in the Grafana service in docker-compose.yml.

apiVersion: 1

contactPoints: []
